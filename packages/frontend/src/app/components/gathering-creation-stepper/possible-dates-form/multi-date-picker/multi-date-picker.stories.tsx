import type { Meta } from '@storybook/react';
import MultiSelectDatePicker from './multi-date-picker';

const meta: Meta<typeof MultiSelectDatePicker> = {
  component: MultiSelectDatePicker,
};
export default meta;

export const Primary = {
  args: {},
};

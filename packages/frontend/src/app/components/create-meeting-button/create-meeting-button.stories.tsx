import type { Meta } from '@storybook/react';
import CreateMeetingButton from './create-meeting-button';

const meta: Meta<typeof CreateMeetingButton> = {
  component: CreateMeetingButton,
  title: 'CreateMeetingButton',
};
export default meta;

export const Primary = {
  args: {},
};

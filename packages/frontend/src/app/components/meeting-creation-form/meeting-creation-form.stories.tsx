import type { Meta } from '@storybook/react';
import MeetingCreationForm from './meeting-creation-form';

const meta: Meta<typeof MeetingCreationForm> = {
  component: MeetingCreationForm,
  title: 'MeetingCreationForm',
};
export default meta;

export const Primary = {
  args: {},
};

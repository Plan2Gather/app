import type { Meta } from '@storybook/react';
import PossibleDatesStep from './possible-dates';

const meta: Meta<typeof PossibleDatesStep> = {
  component: PossibleDatesStep,
};
export default meta;

export const Primary = {
  args: {},
};

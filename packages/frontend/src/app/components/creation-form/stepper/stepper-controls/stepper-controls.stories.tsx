import type { Meta } from '@storybook/react';
import CreationStepperControls from './stepper-controls';

const meta: Meta<typeof CreationStepperControls> = {
  component: CreationStepperControls,
};
export default meta;

export const Primary = {
  args: {},
};

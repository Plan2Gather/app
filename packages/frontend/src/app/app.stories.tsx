import type { Meta } from '@storybook/react';
import App from './app';

const meta: Meta<typeof App> = {
  component: App,
  title: 'App',
};
export default meta;

export const Primary = {
  args: {},
};
